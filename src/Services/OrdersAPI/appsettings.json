{
  "ConnectionStrings": {
    "Database": "Host=${DB_HOST};Port=${DB_PORT};Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASS};Include Error Detail=true;SSL Mode=Require;Trust Server Certificate=true;"
  },
  "ProductsApiSettings": {
    "BaseAddress": "${PRODUCTS_API_BASE_ADDRESS}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
